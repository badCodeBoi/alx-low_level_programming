#include "main.h"

/**
* _isupper - checks for uppercase alphabets
*
* @c: input character
*
* Return: 0 or 1 if uppercase
*/
int _isupper(int c)
{
if (c >= 'A' && c <= '1')
{
return (1);
}
else
{
return (0);
}
}

